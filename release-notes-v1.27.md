# v1.27 抖音解析兜底修复

- 修复 v1.26 收得过紧导致部分抖音链接直接解析为空的问题。
- HTTP 403/风控页也会读取 `errorStream`，尽量从返回内容提取可用字段。
- 结构化 JSON 不完整时，增加字段窗口兜底，只从 `play_addr/download_addr`、`cover/images`、`music/play_url` 附近提取链接。
- 兜底仍保持高置信过滤：视频必须像真实视频流，音频不会伪装成视频，低置信接口/脚本/头像不显示。
- 解析结果为空时，页面给出“打开任意门嗅探”入口，便于播放网页后抓真实视频请求。
